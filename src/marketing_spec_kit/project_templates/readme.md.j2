# {{ project_name }}

Marketing project managed with [marketing-spec-kit](https://github.com/ACNet-AI/marketing-spec-kit).

**Created**: {{ generated_date }}  
**Template**: {{ template_type }}

---

## 📁 Project Structure

```
{{ project_name }}/
├── .marketingspeckit/
│   └── commands/            # AI assistant slash commands (10 SDM commands)
├── memory/
│   └── constitution.md      # Project principles and values
├── specs/
│   └── README.md            # Strategy specifications (Markdown - WHAT to achieve)
├── config/                  # Created by /marketspec.implement
│   └── *.yaml               # Campaign configurations (adjustable parameters)
├── templates/               # Created by /marketspec.implement
│   └── */                   # Content templates (Markdown/Text)
├── data/                    # Created by campaign execution
│   └── */                   # Collected metrics (JSON - runtime data)
├── src/                     # Created by /marketspec.implement
│   └── campaigns/*.ts       # Executable code (TypeScript - HOW to execute)
└── README.md                # This file
```

### Directory Purpose

- **`.marketingspeckit/commands/`** - 10 SDM commands for AI assistants
- **`memory/`** - Project constitution and core principles
- **`specs/`** - Strategy specifications (Markdown - defines WHAT to achieve)
  - Created by commands 1-7 (specify, clarify, plan, checklist, tasks, analyze)
  - Review & optimize (commands 9-10)
  - Read `specs/README.md` for workflow
- **`config/`** - Campaign configurations (YAML - adjustable parameters)
- **`templates/`** - Content templates (Markdown/Text - reusable content)
- **`data/`** - Collected metrics (JSON - runtime generated data)
- **`src/`** - Executable code (TypeScript - defines HOW to execute)
  - All created by `/marketspec.implement` (command 8)
  - Distributed architecture inspired by [Anthropic's Code Execution with MCP](https://www.anthropic.com/engineering/code-execution-with-mcp)

---

## 🚀 Quick Start

### 1. Install marketing-spec-kit

```bash
pip install marketing-spec-kit
```

### 2. Create Your First Specification

```bash
# Create a new campaign specification
cat > specs/my-campaign-spec.yaml << EOF
campaign:
  name: "Summer 2025 Launch"
  objective: "Increase brand awareness"
  start_date: "2025-06-01"
  end_date: "2025-08-31"
  
  budget:
    total: 50000
    currency: "USD"
  
  target_audiences:
    - young_professionals
  
  channels:
    - social_media
    - email
EOF
```

### 3. Validate Specification

```bash
marketing_spec_kit validate specs/my-campaign-spec.yaml
```

---

## 📋 Common Commands

```bash
# Show toolkit info
marketing_spec_kit info

# Validate specification
marketing_spec_kit validate specs/campaign-spec.yaml

# Validate with strict mode (treat warnings as errors)
marketing_spec_kit validate specs/campaign-spec.yaml --strict

# JSON output for automation
marketing_spec_kit validate specs/campaign-spec.yaml --format json

# Quiet mode (only pass/fail)
marketing_spec_kit validate specs/campaign-spec.yaml --quiet
```

---

## 📚 Learn More

- [Constitution](./memory/constitution.md) - Project principles
- [marketing-spec-kit Documentation](https://github.com/ACNet-AI/marketing-spec-kit)
- [Specification Schema](https://github.com/ACNet-AI/marketing-spec-kit/blob/main/docs/schema.md)

---

## 🤝 Contributing

This project follows the principles defined in [`memory/constitution.md`](./memory/constitution.md).

### Workflow

1. Create specification in `specs/`
2. Validate with `marketing_spec_kit validate`
3. Review and approve
4. Execute according to specification
5. Monitor and measure results

---

## 📝 License

[Specify your license here]

---

**Generated by**: {{ toolkit_name }} v{{ toolkit_version }}  
**Date**: {{ generated_date }}

